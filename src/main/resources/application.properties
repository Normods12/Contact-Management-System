
spring.config.import=optional:file:.env[.properties]

spring.application.name=contacts

# Server
server.port=8080

# Spring Security (basic login)
spring.security.user.name=sujal
spring.security.user.password=sujal

# PostgreSQL Database
spring.datasource.url=jdbc:postgresql://localhost:5432/contact_db
spring.datasource.username=postgres
spring.datasource.password=Neha@1234

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# (Optional but clean)
spring.jpa.open-in-view=false